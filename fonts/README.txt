Drop the licensed Monoweb Book font file here as:

    MonowebBook.woff2

It's referenced by @font-face in css/style.css. Until it's present, the site
falls back to 'Courier New', Courier, monospace (per the design system).
